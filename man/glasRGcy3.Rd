% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{glasRGcy3}
\alias{glasRGcy3}
\title{Glas cohort (E-TABM-115): hybridizations with patient RNA labeled with Cy3}
\format{
An \code{RGList} (see \link[limma:RGList-class]{limma::RGList}) with 1900 microarray features and
162 arrays. Components:
\itemize{
\item \code{R}, \code{Rb}: raw median foreground and background intensities of the red
channel;
\item \code{G}, \code{Gb}: raw median foreground and background intensities of the
green channel;
\item \code{logRatio}, \code{logRatioError}: the log ratio between the red and green
channels and its error, as reported in the raw Feature Extraction
files (stored as plain list components, not in \code{other});
\item \code{targets}: a \code{data.frame} with one row per array holding the SDRF
information for the reference channel (hybridization and scan names,
raw file names) and \code{putativeCohort};
\item \code{genes}: a \code{data.frame} with the \code{A-MEXP-318} array design annotation
plus \code{genes231}, \code{genes70} and \code{gns231Cors} (see \link{glasRGcy5});
\item \code{source}: \code{"generic"}.
}
}
\source{
ArrayExpress/BioStudies accession E-TABM-115,
\url{https://www.ebi.ac.uk/biostudies/arrayexpress/studies/E-TABM-115};
raw files archived on Zenodo, \doi{[ZENODO_DOI]} (see
\code{\link[=fetchMammaPrintRaw]{fetchMammaPrintRaw()}}).
}
\usage{
glasRGcy3
}
\description{
A \code{limma} \code{RGList} with the raw two-color intensities for the 162 breast
cancer patients of the Glas et al. (2006) study, restricted to the set of
dye-swap hybridizations in which the information provided in the
ArrayExpress SDRF table refers to the reference pool "MRP" labeled with
Cy3. The SDRF table available from ArrayExpress does not report the
clinical information for this set of hybridizations (its rows describe
the reference channel), hence \code{targets} only carries the reference-pool
and hybridization metadata plus the inferred \code{putativeCohort}. The
arrays are in the same order as in \link{glasRGcy5}, which holds the clinical
information of the corresponding patients.
}
\examples{
library(limma)
class(glasRGcy3)
dim(glasRGcy3)
head(glasRGcy3$targets, n = 5)
## arrays are paired with glasRGcy5 by position
all(glasRGcy3$targets$Hybridization.Name != glasRGcy5$targets$Hybridization.Name)
}
\references{
Glas AM, Floore A, Delahaye LJ, et al. Converting a breast cancer
microarray signature into a high-throughput diagnostic test.
\emph{BMC Genomics} 2006, 7:278. \doi{10.1186/1471-2164-7-278},
\url{https://pubmed.ncbi.nlm.nih.gov/17074082/}.
}
\seealso{
\link{glasRGcy5}, \link{buyseRGcy3}, \link{seventyGeneSignature},
\link[limma:RGList-class]{limma::RGList}
}
\keyword{datasets}
