% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{buyseRGcy5}
\alias{buyseRGcy5}
\title{Buyse cohort (E-TABM-77): hybridizations with the reference RNA labeled with Cy5}
\format{
An \code{RGList} (see \link[limma:RGList-class]{limma::RGList}) with 1900 microarray features and
307 arrays, named \verb{BC.<sample>}. Components:
\itemize{
\item \code{R}, \code{Rb}: raw median foreground and background intensities of the red
channel;
\item \code{G}, \code{Gb}: raw median foreground and background intensities of the
green channel;
\item \code{logRatio}, \code{logRatioError}: the log ratio between the red and green
channels and its error, as reported in the raw Feature Extraction
files (stored as plain list components, not in \code{other});
\item \code{targets}: a \code{data.frame} with one row per array combining the SDRF
rows of the two channels: patient characteristics (age, tumor size and
grade, ER status, MammaPrint, NPI, St Gallen and Adjuvant! Online risk
categories, providing center), the \code{Cy5}/\code{Cy3} sample assignment, and
the curated end points \code{OS}/\code{OSevent}/\code{TenYearSurv} (overall survival,
years), \code{DFS}/\code{DFSevent}/\code{FiveYearDiseaseFree} (disease-free survival),
\code{TTM}/\code{TTMevent}/\code{FiveYearRecurrence} (time to distant metastasis) and
\code{toExclude} (patients with unknown ER status, excluded in the original
analysis);
\item \code{genes}: a \code{data.frame} with the \code{A-MEXP-318} array design annotation
(\code{Reporter.Name}, \code{Comment.AEReporterName}, database entries, control
type, grid position) plus \code{genes231} and \code{genes70} (logical membership
in the 231-gene and 70-gene prognostic lists) and \code{gns231Cors} (the
original van 't Veer correlation with outcome, where available);
\item \code{source}: \code{"generic"}.
}
}
\source{
ArrayExpress/BioStudies accession E-TABM-77,
\url{https://www.ebi.ac.uk/biostudies/arrayexpress/studies/E-TABM-77};
raw files archived on Zenodo, \doi{10.5281/zenodo.22285418} (see
\code{\link[=fetchMammaPrintRaw]{fetchMammaPrintRaw()}}).
}
\usage{
data(buyseRGcy5)
}
\description{
A \code{limma} \code{RGList} with the raw two-color intensities for the 307 breast
cancer patients of the Buyse et al. (2006) TRANSBIG validation study,
restricted to the set of dye-swap hybridizations in which the reference
pool "MRP" was labeled with Cy5 and the patient RNA with Cy3. See
\link{buyseRGcy3} for the swapped hybridizations.
}
\examples{
library(limma)
data(buyseRGcy5)
class(buyseRGcy5)
dim(buyseRGcy5)
head(buyseRGcy5$targets, n = 5)
head(buyseRGcy5$genes, n = 5)
table(
  buyseRGcy5$targets$FiveYearRecurrence,
  buyseRGcy5$targets$Characteristics.BioSourceProvider
)
}
\references{
Buyse M, Loi S, van 't Veer L, et al. Validation and clinical utility of
a 70-gene prognostic signature for women with node-negative breast
cancer. \emph{J Natl Cancer Inst} 2006, 98(17):1183-1192.
\doi{10.1093/jnci/djj329}, \url{https://pubmed.ncbi.nlm.nih.gov/16954471/}.
}
\seealso{
\link{buyseRGcy3}, \link{glasRGcy5}, \link{seventyGeneSignature},
\link[limma:RGList-class]{limma::RGList}
}
\keyword{datasets}
