% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch-raw.R
\name{fetchMammaPrintRaw}
\alias{fetchMammaPrintRaw}
\title{Retrieve the raw data behind the mammaPrintData data sets}
\usage{
fetchMammaPrintRaw(
  accession = c("E-TABM-77", "E-TABM-115", "seventyGene"),
  source = c("zenodo", "biostudies"),
  files = NULL,
  cache = BiocFileCache::BiocFileCache(),
  verbose = interactive()
)
}
\arguments{
\item{accession}{Which resource to fetch: \code{"E-TABM-77"}, \code{"E-TABM-115"}
or \code{"seventyGene"}.}

\item{source}{\code{"zenodo"} (default) or \code{"biostudies"}.}

\item{files}{Only for \code{source = "biostudies"}: character vector of raw
file names to download in addition to the SDRF and ADF (as listed in
the \verb{Array Data File} column of the SDRF; a trailing \code{.gz} is
tolerated), or \code{"all"} for the complete set. Ignored for Zenodo.}

\item{cache}{A \link[BiocFileCache:BiocFileCache]{BiocFileCache::BiocFileCache} object. Defaults to the
user's default cache.}

\item{verbose}{Logical, print download messages.}
}
\value{
A named character vector with the local paths of the requested
files. Names are the original file names.
}
\description{
Downloads (once) and caches the original files from which the processed
data sets of this package were generated, and returns their local paths:
\itemize{
\item \code{"E-TABM-115"}: the Glas et al. (2006) cohort (\link{glasRGcy5},
\link{glasRGcy3}): SDRF sample table, \code{A-MEXP-318} array design (ADF) and
324 Agilent Feature Extraction raw files;
\item \code{"E-TABM-77"}: the Buyse et al. (2006) cohort (\link{buyseRGcy5},
\link{buyseRGcy3}): SDRF, ADF and 614 raw files;
\item \code{"seventyGene"}: the van 't Veer et al. (2002) supplementary table
\verb{415530a-s9.xls} behind \link{seventyGeneSignature}.
}

Two sources are available:
\itemize{
\item \code{"zenodo"} (recommended): one gzip-compressed tar archive per
ArrayExpress accession (\verb{E-TABM-115_raw.tar.gz}, 70 MB, and
\verb{E-TABM-77_raw.tar.gz}, 134 MB), each containing the SDRF, the ADF and
all gzipped raw files exactly as originally retrieved from
ArrayExpress, plus the spreadsheet. The function returns the path to the
archive (or spreadsheet); extract it with \code{\link[utils:untar]{utils::untar()}}. Files are
served from Zenodo record \doi{[ZENODO_DOI]}.
\item \code{"biostudies"}: the per-file tree served by EBI BioStudies (the current
home of ArrayExpress). Only the SDRF and the ADF are downloaded by
default; individual raw files (uncompressed, several hundred of them
per accession, about 820 KB each) can be requested with \code{files}. The
spreadsheet is not available from this source.
}

Downloads are managed by \code{BiocFileCache}, so repeated calls return the
cached copies without touching the network.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
## The Buyse cohort archive from Zenodo (134 MB, downloaded once)
archive <- fetchMammaPrintRaw("E-TABM-77")
rawDir <- tempfile("E-TABM-77_")
untar(archive, exdir = rawDir)
list.files(rawDir, recursive = TRUE)[1:5]

## The sample table and array design only, from BioStudies
meta <- fetchMammaPrintRaw("E-TABM-115", source = "biostudies")
sdrf <- read.delim(meta[["E-TABM-115.sdrf.txt"]], check.names = FALSE)
head(sdrf[["Array Data File"]])

## Two raw files from BioStudies
fetchMammaPrintRaw("E-TABM-115",
  source = "biostudies",
  files = sdrf[["Array Data File"]][1:2]
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[BiocFileCache:bfcrpath]{BiocFileCache::bfcrpath()}}, \code{\link[utils:untar]{utils::untar()}}
}
